# Ajouter le contenu correct à vercel.json
Add-Content -Path vercel.json -Value @"
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
"@